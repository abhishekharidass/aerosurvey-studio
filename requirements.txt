# GUI
PySide6>=6.6

# Imaging / EXIF
Pillow>=10.0
piexif>=1.1        # optional: write GPS EXIF into the sample dataset

# Geospatial + point clouds
numpy>=1.24
pyproj>=3.5        # coordinate reference transforms (UTM / EPSG)
rasterio>=1.3      # GeoTIFF DSM/DTM/orthomosaic I/O
laspy>=2.5         # LAS/LAZ point clouds
open3d>=0.17       # interactive 3D point-cloud viewer
scikit-learn>=1.3  # trained Random Forest point-cloud classifier (+ joblib)

# --- External engines (not pip-installable; detected on PATH if present) ---
#   COLMAP     https://colmap.github.io/           feature matching + SfM/AT
#   OpenMVS    https://github.com/cdcseacave/openMVS  dense multi-view stereo
#   PDAL       https://pdal.io/                     point-cloud classification
#   GDAL       https://gdal.org/                    raster/ortho export
